import XCTest
@testable import VideoPlayerViews

final class VideoPlayerViewsTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(VideoPlayerViews().text, "Hello, World!")
    }
}
